// BDD Assertions (Chai)
// expect/should

// TDD Assertions (Chai)
// assert

// Chai-jQuery
// when asserting about a DOM object
// attr, css, class, text

// Sinon-Chai
// Used on assertions with cy.stub() and cy.spy()

// Examples:
// cy.contains('[data-cy=day]', "Monday").should('be.visible');
// cy.url().should('include', '/game');
// cy.get('[data-cy=sessions]').should('have.length', 10);
// cy.get('data-cy=profile]').should('not.exist');